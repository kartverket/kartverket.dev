apiVersion: backstage.io/v1alpha1
kind: Location
metadata:
  name: local-development-catalog
  description: Synthetic catalog data for credential-free local development
spec:
  type: file
  targets:
    - ./organization.yaml
    - ./domains-and-systems.yaml
    - ./components.yaml
    - ./apis-and-resources.yaml
    - ./functions.yaml
